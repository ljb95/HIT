<?php

require_once("../../../../config.php");

    $event = optional_param('event_num', 0, PARAM_INT); 
    $st_time = optional_param('st_time', 0, PARAM_INT); 
    $lrn_time = optional_param('duration_num', 0, PARAM_INT); 
    $total_time = optional_param('event_num', 0, PARAM_INT); 

    
    echo $event;
    
    if($event){
        
    }
    